import { create } from "zustand";

// type availableTheme = "light" | "dark";
// const appTheme = create((set) => ({
//   theme: "light",
//   toggleTheme: () =>
//     set((state) => {
//       if (state.theme == "light") {
//         return state.theme == "dark";
//       }
//       if (state.theme == "dark") {
//         return (state.theme = "light");
//       }
//     }),
// }));

